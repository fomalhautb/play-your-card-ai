node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# 卡面图集（`pnpm assets:build` 的产物）。源头是 public/cards 下的原画，随时能重打，
# 而图集本身有几兆，进仓库只会把每次改原画都变成一次大 diff。
# assets/dist/ 已经被上面的 dist/ 覆盖，这两条是脚本分发过去的副本。
apps/web/public/atlas/
packages/bench/public/atlas/

# AssetPack 的增量缓存目录，在哪儿跑就落在哪儿。
.assetpack/

# wrangler dev 的本地状态：构建缓存，以及 miniflare 给 Durable Object 建的 SQLite 库。
# 纯本地产物，每台机器都不一样，删掉下次 `wrangler dev` 会重新生成。
.wrangler/

.DS_Store
.idea/
.vscode/

*.log
npm-debug.log*
pnpm-debug.log*

.env
.env.local
.env.*.local

.pnpm-store/
tmp/
output/
scripts/out/pregen-smoke.json
