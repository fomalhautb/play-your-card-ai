/**
 * 设计令牌：卡牌和版式的公共尺寸，外加动效时长。
 *
 * 全项目只在这里定义一次，画布那边的 Pixi 组件直接读它。
 * 允许依赖：无。这是最底层的包，谁都可以依赖它，它不依赖任何人。
 *
 * 值的来源是 tokens/*.json，产物由 sd.config.mjs 生成，别改 src/generated 下的文件。
 * 尺寸是纯数字（px），时长是秒——Pixi 只认数字，GSAP 的 duration 也是秒。
 * 怎么改、收了什么、明确不收什么，见 README.md。
 *
 * 正式版简化第 5 步把颜色、不透明度、字体、圆角、间距五组全删了（CSS 产物跟着一起删）：
 * React 那边剥成素方块之后一条 `var(--…)` 都不剩，画布那边剩下的颜色只服务卡面和几层
 * 全屏遮罩、各自只有一两个调用方，按 README「明确不收什么」的判据就地留在组件里。
 * 重做视觉时会重新定一套色板，那时再决定哪些值值得收回来。
 */

export { type Tokens, tokens } from './generated/tokens'
