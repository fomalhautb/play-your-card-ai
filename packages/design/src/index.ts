/**
 * 设计令牌：颜色、字号、间距、圆角、动效时长。
 *
 * 全项目只在这里定义一次，`canvas` 的 Pixi 组件和 `ui` 的 React 组件共用同一份数值，
 * 组件里禁止写死颜色和时长（见《正式版架构》7.1 第 4 条）。
 * 允许依赖：无。这是最底层的包，谁都可以依赖它，它不依赖任何人。
 *
 * 令牌在迁移第 8 条从旧的 styles.css 里抠出来，现在只有骨架。
 */

export {}
