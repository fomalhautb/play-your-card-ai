/**
 * 卡牌、英雄、题库、预生成答案这些游戏内容数据，以及校验它们的 zod schema。
 *
 * 只放数据和数据的形状，不放规则：规则在 `core`，它是纯函数、不带数据。
 * 允许依赖：`core`（用它的类型描述卡牌效果）。除此以外谁都不依赖。
 *
 * 数据本体在迁移第 12 条从 legacy-client 和 core 里搬过来，现在只有骨架。
 */

export {}
