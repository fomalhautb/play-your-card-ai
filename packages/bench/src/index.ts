/**
 * 性能剧本：脚本化的操作序列、固定步进时钟、WebGL 计数器，对应《正式版架构》6.9 的确定性指标。
 *
 * 它是测量工具不是游戏的一部分，只被 CI 和本地压测调用，不进任何平台的构建产物。
 * 允许依赖：`canvas`（要驱动真实场景）、`design`、`platform`。
 *
 * 剧本在迁移第 2 条搭起来、第 20 条接进 CI。现在只有骨架。
 */

export {}
