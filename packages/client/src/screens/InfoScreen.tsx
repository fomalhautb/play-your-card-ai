/**
 * 关于本作（`/info`）。首页菜单里「关于」那一项进来。
 *
 * 内容只回答两件事：**这游戏怎么玩**、**这东西是谁做的**。
 * 旧版这一页只有后半段（团队名单和黑客松出处），玩法说明散在首页和对局里；
 * 正式版的首页整个画在画布上，没有地方写字，所以玩法那一段搬到这里来。
 *
 * 正式版简化第 3 步之前，这一页整个压在一张背景图上，还要先过一遍加载页等那张图。
 * 背景图是纯装饰，剥样式时一起去掉了，等图的闸门也就跟着没了意义——
 * 这一页现在没有任何要等的素材，进来就能读。第 5 步把那张图连源文件和清单一起删了。
 *
 * 页眉上原先还有一颗静音。现在右上角那颗是全站常驻的（见 app/MuteButton.tsx），
 * 这一页留着就是同一件事摆两颗，所以删了。
 */

import { Notice, Page } from '@ai-duel/ui'
import { useLocation } from 'wouter'

/** 团队名单，不写分工——四个人的活儿是混着干的（抄旧版的注释）。 */
const TEAM = ['石在', '司马冰清', '刘利剑', '叶丁元']

/** 玩法说明。一条一句话，多了没人看。 */
const RULES = [
  '每一轮先出牌：把手里的 AI 牌放到自己这半边战场上，费用不够就放不下。',
  '出完牌亮题，场上每张 AI 牌各答一道，答对的给自己这边记一分。',
  '技能牌不上场，打出去当场结算；英雄技能一局只能发一次。',
  '先拿到足够分数的一方赢下整局。',
]

export function InfoScreen() {
  const [, navigate] = useLocation()

  return (
    <Page title="关于本作" onBack={() => navigate('/')}>
      <h2>怎么玩</h2>
      <ol>
        {RULES.map((rule) => (
          <li key={rule}>{rule}</li>
        ))}
      </ol>

      <h2>开发团队</h2>
      <p>出牌吧！AI！ 由下面四个人做出来：</p>
      <p>{TEAM.join(' · ')}</p>
      <p>SheNicest 2026 年 8 月黑客松 · 五天之内完成。</p>

      <Notice tone="info">感谢你的游玩与支持。</Notice>
    </Page>
  )
}
