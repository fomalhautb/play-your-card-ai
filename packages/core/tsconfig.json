{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    // 规则引擎必须能在浏览器和 Node 两边跑，这里清空全局类型，
    // 误用 DOM 或 Node API 时会直接编译报错。
    "types": []
  },
  // 测试单独走 tsconfig.test.json：那份多开一个 node 类型，给回放和 golden 录制读写文件用。
  "include": ["src"]
}
