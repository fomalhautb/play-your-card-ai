/**
 * 平台能力接口，以及 web / electron / capacitor 三套实现。
 *
 * 网络、音频、存储、图片加载、全屏、安全区、触感都从这里走。场景代码不直接碰浏览器对象，
 * 换平台时只换实现（见《正式版架构》第 2 节第 5 条）。
 * 允许依赖：无。它是被 `canvas`、`ui`、`client`、`bench` 依赖的底层包。
 *
 * 接口和 web 实现在迁移第 11 条定下来，electron、capacitor 实现在第 35、36 条补。
 * 现在只有骨架。
 */

export {}
