/**
 * Pixi 组件库、各个画布场景，以及演出编排。
 *
 * 卡牌绝不进 DOM，带大动画的界面（对局、牌组编辑、开包、主页人物）整个在这里画。
 * 允许依赖：`design`（令牌）、`platform`（平台能力）。
 * 不依赖 `ui`——画布和 React 是并列的两套组件库，互相不引用（见《正式版架构》7.2 第 1 条）。
 *
 * 组件按场景需要逐步补，从迁移第 17 条开始。现在只有骨架。
 */

export {}
